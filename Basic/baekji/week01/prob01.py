A,B=input().split()

A=int(A)
B=int(B)

print(A+B)
print(A-B)
print(A*B)
print(int(A/B))
print(A%B)

'''
Comment:

A//B가 더 효율적입니다.
A/B는 실수 연산 후 int 변환이 필요해 연산이 2번 일어납니다.
A//B는 한 번에 정수 몫을 구합니다(더 빠름).

1. int(A/B)
- A와 B를 나눈 결과(실수)를 int()로 감싸 소수점 이하를 버립니다(내림이 아님, 0쪽으로 절삭).
예: int(-3/2) → int(-1.5) → -1

2. A//B
- 파이썬의 '정수 나눗셈(몫 연산자)'로, 항상 내림(floor) 연산을 합니다.
예: -3//2 → -2 (항상 아래쪽 정수로 내림)
- 즉, 음수일 때 결과가 다를 수 있습니다.
'''